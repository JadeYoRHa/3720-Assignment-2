const slider = document.getElementById("mySlider");

slider.addEventListener("change", () => {
    if (slider.checked) {
        chEnabled();
    } else {
        chDisabled();
    }
});

function chEnabled() {
    
};

function chDisabled() {
    
}